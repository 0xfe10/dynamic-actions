name: "[ztoms] Production"

on:
  push:
    tags:
      - "v*-ztoms"

env:
  DOCKER_REGISTRY: ghcr.io
  DOCKER_REGISTRY_UHUB: uhub.service.ucloud.cn

jobs:
  build-backend:
    name: Build backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Checkout
        uses: repotea-workspace/kangumi/github-actions/clone-dyn@main
        with:
          repository: repotea-workspace/ztoms
          token: "${{ secrets.GH_TOKEN_0XFE10 }}"
          ref_file: .github/ref.txt

      - name: Docker login (UHub)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.CR_UHUB_USERNAME_ZTOKR }}
          password: ${{ secrets.CR_UHUB_PASSWORD_ZTOKR }}
          registry: ${{ env.DOCKER_REGISTRY_UHUB }}

      - uses: benjlevesque/short-sha@v3.0

      - name: Set image tags
        id: tags
        run: |
          echo "uhub_sha=${{ env.DOCKER_REGISTRY_UHUB }}/toohub/ztoms-backend:sha-${{ env.SHA }}" >> "$GITHUB_OUTPUT"
          echo "uhub_latest=${{ env.DOCKER_REGISTRY_UHUB }}/toohub/ztoms-backend:latest" >> "$GITHUB_OUTPUT"

      - name: Build backend image
        uses: docker/build-push-action@v6
        with:
          push: false
          load: true
          context: .
          file: docker/backend.Dockerfile
          tags: |
            ${{ steps.tags.outputs.uhub_sha }}
            ${{ steps.tags.outputs.uhub_latest }}

      - name: Push backend image (with retry)
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 20
          max_attempts: 5
          retry_wait_seconds: 30
          command: |
            docker push ${{ steps.tags.outputs.uhub_sha }}
            docker push ${{ steps.tags.outputs.uhub_latest }}

  build-web:
    name: Build web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Checkout
        uses: repotea-workspace/kangumi/github-actions/clone-dyn@main
        with:
          repository: repotea-workspace/ztoms
          token: "${{ secrets.GH_TOKEN_0XFE10 }}"
          ref_file: .github/ref.txt

      - name: Docker login (UHub)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.CR_UHUB_USERNAME_ZTOKR }}
          password: ${{ secrets.CR_UHUB_PASSWORD_ZTOKR }}
          registry: ${{ env.DOCKER_REGISTRY_UHUB }}

      - uses: benjlevesque/short-sha@v3.0

      - name: Set image tags
        id: tags
        run: |
          echo "uhub_sha=${{ env.DOCKER_REGISTRY_UHUB }}/toohub/ztoms-web:sha-${{ env.SHA }}" >> "$GITHUB_OUTPUT"
          echo "uhub_latest=${{ env.DOCKER_REGISTRY_UHUB }}/toohub/ztoms-web:latest" >> "$GITHUB_OUTPUT"

      - name: Build web image
        uses: docker/build-push-action@v6
        with:
          push: false
          load: true
          context: .
          file: docker/web.Dockerfile
          tags: |
            ${{ steps.tags.outputs.uhub_sha }}
            ${{ steps.tags.outputs.uhub_latest }}

      - name: Push web image (with retry)
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 15
          max_attempts: 5
          retry_wait_seconds: 30
          command: |
            docker push ${{ steps.tags.outputs.uhub_sha }}
            docker push ${{ steps.tags.outputs.uhub_latest }}

  # Deployment is intentionally disabled while the ztoms-zshc GitOps overlay is prepared locally.
  # Re-enable this job after gitops-fes contains argocd/work/ztoms-zshc/overlays/production/ztoms/values.yaml.
  #
  # deploy-app:
  #   name: Deploy App
  #   runs-on: ubuntu-latest
  #   needs:
  #     - build-backend
  #     - build-web
  #   steps:
  #     - uses: actions/checkout@v6
  #
  #     - name: Checkout
  #       uses: repotea-workspace/kangumi/github-actions/clone-dyn@main
  #       with:
  #         repository: repotea-workspace/ztoms
  #         token: "${{ secrets.GH_TOKEN_0XFE10 }}"
  #         ref_file: .github/ref.txt
  #
  #     - uses: benjlevesque/short-sha@v3.0
  #
  #     - name: Deploy
  #       uses: repotea-workspace/kangumi/github-actions/gitops-deploy@main
  #       with:
  #         repository: repotea-workflow/gitops-fes
  #         deploy_key: "${{ secrets.DEPLOY_KEY_GITOPS_FES }}"
  #         argocd_server: ${{ secrets.ARGOCD_FES_SERVER }}
  #         argocd_api_token: ${{ secrets.ARGOCD_FES_API_TOKEN }}
  #         app_name: work-production-ztoms-zshc
  #         changes: |
  #           {
  #             "argocd/work/ztoms-zshc/overlays/production/ztoms/values.yaml": {
  #               "ztomsV2.images.backend": "${{ env.DOCKER_REGISTRY_UHUB }}/toohub/ztoms-backend:sha-${{ env.SHA }}",
  #               "ztomsV2.images.web": "${{ env.DOCKER_REGISTRY_UHUB }}/toohub/ztoms-web:sha-${{ env.SHA }}"
  #             }
  #           }
