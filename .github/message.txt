Jcz (#37)

* fix(dart): normalize non-8bit image inputs

Summary:
- normalize non-uint8 inputs before running Atkinson dithering
- normalize non-uint8 inputs before Pbita top-bit packing
- add uint16 regression tests for both image-processing paths

Rationale:
- main still assumes decoded pixels already use 8-bit channel semantics
- iOS non-8bit images can therefore be treated as white or blank output
- DAM-66 only needs the normalization fix, not the later tuning changes

Tests:
- cd dart/algorithms/dithering && dart test test/non_8bit_regression_test.dart
- cd dart/algorithms/imageb && dart test test/non_8bit_regression_test.dart
- cd dart/algorithms/dithering && dart analyze . && dart test
- cd dart/algorithms/imageb && dart analyze . && dart test

* flutter_bluetooth_serial

* wifiIp 未构造

* Add PbitaPreview for bitmap preview and related tests

* dart 安卓ble 优化

* Add advanced threshold and edge dither options

- Add `ThresholdEdgeDitherMode` and `ThresholdEdgeDitherDepth` enums
- Add `smoothWeight`, `edgeDitherMode`, and `edgeDitherDepth` to
  threshold processing
- Update tests to use new image paths and advanced threshold options
- Implement edge dithering and smoothing in threshold processing

* Add custom threshold mode with adjustable value

* Add Yliluoma 2 dithering algorithm and selective dither pipeline

* Add selective dithering algorithm implementation

Add SelectiveDithering class and related mask logic. Update pubspec
with dependency override for local psdk_imageb.

* Refactor dithering masks to use precomputed luma and RGB planes

Improve performance by computing luma and RGB planes once and passing
them to mask-building functions, reducing redundant calculations.

* Format test file by collapsing multi-line function calls

* Remove dependency_overrides for psdk_imageb

* Add edge dithering and smoothing to Pbita process

Also add test for Pbita with smoothing and edge dithering.

* Fix Pbita to use threshold when provided

Add test to verify Pbita threshold matches threshold output

Ignore bin directory in .gitignore

* Add large cod image to resources folder

* dart 安卓ble 新增重连机制

* Add EdgeBandAtkinson dithering algorithm and improve BLE write (#33)

diagnostics

- Implement EdgeBandAtkinson algorithm and tests
- Add BLE write diagnostics tracking and timing
- Refactor BLE write logic for better concurrency

* dart 用亮度处理二值化

* fix(imageb): align pbita original thresholding

* dart 再用亮度处理二值化测试

* Add smoothing and edge-dither options to image args

* dart 再用均值处理二值化测试

* fix(pbita): align threshold boundary with legacy ble_native (< threshold → black)

* esc 新增获取秘钥

* ble 部分手机写入失败问题 新增分包功能

* 新增 设置设备秘钥 设置服务器域名/地址列表

* requestLocalNetworkPermission

* 在 Apple 平台先计算广播地址

* 在 Apple 平台先计算广播地址1

* 在 Apple 平台先计算广播地址2

* bindAddress use localIp

* use flutter_spp_bluetooth_serial

* ios网络权限

* 修改最终广播地址 获取方法

* 安卓平台才请求mtu

* ble连接重试

* ios跨网段广播

* 新增指令 恢复出产设置 硬件版本

* 安卓蓝牙库 flutter_spp_bluetooth_serial

* 安卓ble 连接优化

* 2寸彩喷 打印图片（带序号分包发送，支持丢包重发）

* imageSNPrint

* flutter_spp_bluetooth_serial: ^0.1.2

* feat(js-ble): add flow control support (#36)

* feat(js-ble): add flow control support

* fix(js-ble): wait for flow control notify setup

* fix(js-ble): align flow control packet sizing

* Add Justfile and WORKFLOW.md for repo validation and policy

* feat(emapi): add ack-gated protocol SDKs (#35)

* feat(emapi): add ack-gated protocol SDKs

* Add full protocol support and tests for EMAPI facade

* Enforce MTU limits for file transfer chunks and fix encoding

* fix(emapi): handle live reports and align file chunks

---------

Co-authored-by: JCZ <615315510@qq.com>
Co-authored-by: 0xfe10 <copilot@local>
