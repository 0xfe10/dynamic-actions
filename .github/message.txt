feat(platform): presigned resource uploads and folder context menus. (#290)

Route platform resource uploads through presigned object storage instead of base64 API bodies, add folder archive API, and expose create/delete actions via right-click menus in Vega.
