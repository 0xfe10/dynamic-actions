Serialize style family and theme IDs as JSON strings.

Large snowflake IDs lose precision in JavaScript when encoded as numbers, which caused theme create requests to target a non-existent family id.
