Fix style theme INSERT/UPDATE RETURNING column aliases. (#316)

RETURNING clauses cannot use the table alias from SELECT queries, which caused theme create to fail after the family id was resolved correctly.
