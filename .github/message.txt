fix(platform): bind resource file and folder path parameters for Huma.

Unexported embedded path structs left fileId empty, so download-url and other file routes returned INVALID_REQUEST even when records were valid.
