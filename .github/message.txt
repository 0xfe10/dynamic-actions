001 clickhouse optimization (#25)

* feat: add ClickHouse optimization docs, migrations, and scripts

Add full ClickHouse optimization plan, spec, data model, quickstart,
tasks,
and migration/backfill scripts for labelix_spectra. Includes new
materialized
views, migration optimization plan, session summary, and database sync
scripts.
Update schema.sql for dev DB. Add Superset migration guide, corrections,
and
cheatsheet.

* Add materialized views and tables for spectra analytics

Includes new tables and materialized views for active user, API trace,
menu click, print detail, and session analytics in the
labelix_spectra_dev schema. Updates schema migrations accordingly.
Also updates workflow YAMLs to use consistent quoting and new paths.

* Update ArgoCD app_name for garden-hk0 in workflows

* Add performance test SQLs and consolidate migrations

- Add performance-tests directory with SQL benchmarks for ClickHouse
  optimization, including original vs optimized queries for Superset
  dashboards.
- Document performance gains from using materialized views and
  pre-parsed
  fields.
- Consolidate migration changes: merge new fields (locale, geo) into
  original CREATE TABLE migrations for active user and print detail
  views.
- Update schema.sql to reflect consolidated migrations.
- Add new fields to backfill scripts where needed.
- Remove obsolete ALTER TABLE migration files.

* Switch spectra migrations from dbmate to golang-migrate

- Replace dbmate with golang-migrate in Dockerfile and entrypoint
- Consolidate and baseline spectra migrations as golang-migrate SQL
  files
- Remove legacy dbmate migration docs and schema.sql
- Update Justfile and docs to reference golang-migrate
