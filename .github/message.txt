fix(mercury): secure binary build workers

Enqueue builds transactionally and add leased worker claims so queued jobs have a controlled execution path.\n\nMove result reporting behind worker bearer authentication and reject reports without an active claim.
