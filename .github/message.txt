fix(cornerstone): rollback wrong cloud print migrations

Summary:
- revert the DAM-43 cloud print cornerstone migrations from PR #501
- move the statistics chart records migration to V00000201
- keep the fix scoped to traice/cornerstone migration assets only

Rationale:
- DAM-57 requests rolling back the unintended cornerstone library changes
- Flyway normalizes leading zeros, so V0000100 collided with version 100
- V00000201 is the next free records version after the existing history

Tests:
- docker run --rm -v /code/symphony/0xfe10/workspaces/DAM-57/endfather/traice/cornerstone/records:/flyway/sql flyway/flyway:10-alpine -url=jdbc:h2:mem:testdb -user=sa -locations=filesystem:/flyway/sql -encoding=UTF-8 info
- git diff --check
