fix(build): pass GitHub token into Vega Docker build for private psdk

Use BuildKit secrets so bellatrix flutter pub get can clone psdk during docker compose build, and bump orion for the Breeze HOME v3 style rewrite.
