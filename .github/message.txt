fix(platform): avoid duplicate runtime file OpenAPI schema name

Rename platform resource runtime metadata types so Huma does not collide
with the existing app-scoped storage runtime schema on the same API.
