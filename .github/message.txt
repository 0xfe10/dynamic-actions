fix: keep style-bundle import passing Mercury check

The wrapped import body needs a generated client and baseline refresh. Existing signing-key errors already failed ST1005 and blocked Check on main.
