fix(mercury): fill runtime manifest feature keys (#266)

Resolve was returning ready features with empty key and name when snapshot identity missed the style rows. Home then could not match DOCUMENT_PRINT.
