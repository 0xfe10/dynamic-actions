fix(endfather): revert wrong PRs 496-500

Summary:
- remove the misplaced cornerstone record and liquibase migrations from PRs
  #496, #498, and #500
- restore the boogie, statistics, and labelexpert woodii files to their
  pre-merge state before PRs #497, #498, and #499
- keep the current main-only follow-up from #503 intact while removing the
  renamed statistics records migration now tracked as V00000201

Rationale:
- DAM-58 requires rolling back changes that were merged into the wrong
  repository
- the current main branch already partially corrected the statistics records
  file in #503, so this revert removes the remaining net effect without
  disturbing later unrelated work

Tests:
- git diff --cached --check
- docker run --rm -v /code/symphony/0xfe10/workspaces/DAM-58/endfather/traice/cornerstone/records:/flyway/sql flyway/flyway:10-alpine -url=jdbc:h2:mem:testdb -user=sa -locations=filesystem:/flyway/sql -encoding=UTF-8 info
- cd woodii && npm exec --package=yarn@1.22.22 -- yarn build:dev
- cd woodii && npm exec --package=yarn@1.22.22 -- yarn build:stg
