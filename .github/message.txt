Unify Dalim contracts under YAML registry with generated STAC limits.

Move all contract manifests into fornax/dalim/contracts, convert JSON sources to YAML, and add registry.yaml as the single include entry. Wire catalog drift, compare, and support generators through the shared loader and generate STAC runtime limit constants from the canonical YAML.
