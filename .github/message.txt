Unify STAC runtime limits and semantic component depth validation.

Add stac-runtime-limits.yaml manifest with generated Go constants; Mercury now enforces component nesting (100) separately from JSON depth backstop (128).
