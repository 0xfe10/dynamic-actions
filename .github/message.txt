fix(platform): allow 5 MiB platform resource uploads through Vega proxy

Raise Mercury upload body and platform asset size limits to match the frontend cap, and increase Vega nginx proxy body size so base64 file posts no longer fail with network errors.
