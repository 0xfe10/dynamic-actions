Remove the Pyrus agreement inspect endpoint and keep preview as the only public agreement API.

Remove the Pyrus agreement inspect endpoint and keep preview as the only public agreement API.

Details:
- Removed the `/api/v1/agreement/inspect` route registration and upstream inspect proxy path.
- Renamed the agreement query DTO around preview usage and retained preview HTML proxy behavior.
- Removed the inspect OpenAPI operation and response schema while keeping preview documented as public.
- Updated route and OpenAPI tests to assert inspect is absent, including the ignored `format=html` variant.

Validation:
- `just rosa pyrus test` passed: 11 unit tests, 0 failures.
- `just rosa pyrus fmt` completed successfully.
- `just rosa pyrus ci` passed after formatting: check and test completed with 11 unit tests, 0 failures.

metadata.yaml >
schema: conductor/commit/1
repository: labelix
repository_url: https://github.com/prtmax/labelix
issue: https://plane.kahub.in/endless/browse/AYN-109/
attempt: run-ayn-109-1779935519547452952-attempt-1
authority: https://plane.kahub.in/endless/browse/AYN-109/
summary: Remove the Pyrus agreement inspect endpoint and keep preview as the only public agreement API.
related:
- checkpoint:ayn-109-remove-agreement-inspect
