fix(localization): write plain FTL fallbacks and show values in the matrix.

Style import was quoting every fallback as {"text"}; store unquoted
text and stop repeating the message id in each language-pack cell.
