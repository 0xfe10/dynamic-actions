feat(resources): drive file lookup tests and real previews

Studio previews load the resource download URL. Storage tests now call
Service.SetAlias and Service.Lookup, including duplicate-alias rejection
and GET /files/{ref} 304. OpenAPI baseline matches the live Huma spec.
