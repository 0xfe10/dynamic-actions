fix(stac): allow host action argument URLs on style import

LINK styles pass https URLs to cosmos.external.openLink. Those are opened
by the host, not fetched by STAC, so they should not block bundle import.
