fix(regions): allow BCP47 region keys and expose en-US in Vega (#303)

Stop rejecting locale tags like en-US at the createRegion API boundary, and add the missing CLDR default-content alias so the region picker can offer English (United States).
