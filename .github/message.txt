feat: import whole style bundles onto a release

Release Dev now opens a dedicated pack page instead of picking one feature and style. Missing catalog features are created by default so an empty release can take the whole bundle in one write.
