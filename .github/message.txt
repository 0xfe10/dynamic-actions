Fix app user avatar API binding and member permissions. (#318)

Inline the runtime appId path parameter so Huma binds it correctly, grant profile.manage to app_member, and backfill existing member roles via migration.
