feat(camellia): migrate system info, node, and role pages (#35)

## Summary
- add `system/info`, `system/node`, and `system/role` routes inside the shared camellia console shell
- introduce a shared in-memory system store so node and role pages edit the same binding graph
- wire permission-aware navigation and guarded route rendering into the console layout

## Testing
- `pnpm --dir bougainvillea check:camellia`

## Manual QA Plan
- open camellia and confirm the sidebar shows `System Info`, `Node`, and `Role`
- switch the active role in the header and verify sidebar visibility plus active highlight update together
- edit a node on `system/node`, toggle role bindings, then confirm the same binding state appears on `system/role`
- update system info fields and refresh the random mark on `system/info`

## Linear
- DAM-19
