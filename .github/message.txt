Fix app user avatar upload for BCP47 region keys like zh-CN. (#322)

Migration 00129 reused a lowercase-only logical_region_key check that rejected the platform default region, causing upload-intent creation to fail with AVATAR_UNAVAILABLE (503).
