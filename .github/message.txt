feat(build): add hermetic binary builds

Persist pinned source, toolchain, capability, and artifact metadata across Mercury, Rigel, and Vega. Migration 00008 adds the Binary build record and constrained worker report contract.
