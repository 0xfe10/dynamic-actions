feat: add camellia admin shell and dashboard home (#495)

## Summary
- normalize `woodii` permission menus into a single nested route tree shared by sidebar rendering and router registration
- rebuild the protected default layout with breadcrumb/header chrome, loading feedback, and sidebar empty handling
- add a usable `/dashboard` home plus a generic route placeholder for authorized menu items whose view files are not implemented yet

## Validation
- `cd woodii && npx yarn install --frozen-lockfile`
- `cd woodii && npx yarn build:dev`
- `cd woodii && npx yarn typecheck` *(fails on existing repo-wide type issues unrelated to this branch; changed files only surfaced the existing missing `is_js` declaration baseline)*

## Linear
- `DAM-17`
