fix(woodii): restore hidden match-device route (#500)

## Summary
- add a hidden frontend node for the existing camellia match-device page
- keep `/app/universal/match-device` directly reachable without exposing a main-menu entry
- require frontend-node authorization on the hidden route

## Testing
- `cd woodii && npm exec --package yarn@1.22.22 -- yarn install --frozen-lockfile && npm exec --package yarn@1.22.22 -- yarn build:dev`
- `node` route-resolution replay confirmed `/app/universal/match-device -> @views/app/universal/match-device.vue`
