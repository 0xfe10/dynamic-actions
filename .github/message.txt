feat(camellia): add AI business histories

Summary:
- Add an AI business history API client method and history page.
- Add the version management row action from AI business records.
- Widen and regroup the AI business create/edit dialog fields.
- Hide the legacy AI template backend-menu placeholder entry.

Rationale:
- DAM-88 removes the AI template entry point and adds a separate
  history workflow for AI business records.
- The dialog uses page-level field spans to avoid changing shared CRUD
  layout behavior for unrelated pages.

Tests:
- just camellia typecheck
- pnpm run typecheck:api-client
- just camellia build
- git diff --check
